### Project Overview

Winteq Predictive Maintenance is a web-based dashboard application built with Flask and Chart.js. It monitors industrial induction motors by tracking sensor telemetry, predicting failures using trained machine learning models, and generating automated maintenance alerts.

---

### Key Features

- **Dashboard**: Displays real-time health scores, remaining useful life (RUL), and multi-parameter sensor trend analysis.
- **Motor Assets**: Provides a fleet-wide overview with filtering options and dynamic health progress bars.
- **Sensor Data**: Tracks live telemetry for temperature, vibration, voltage, and rotational speed per motor.
- **AI Alerts**: Logs automated diagnostic insights categorized by severity level, generated from real model predictions.
- **Reports**: Generates downloadable PDF maintenance reports with customizable filters (motor, date range, fields).
- **Authentication**: Login/register system with admin and standard user roles.
- **Settings**: Allows customization of alert thresholds and configuration of AI model parameters (admin only).

---

### Prerequisites

Ensure you have Python 3.10+ installed. Install the required dependencies using the terminal command below:

```bash
pip install -r requirements.txt
```

---

### Project Structure

```text
predictive-maintenance/
│
├── app.py                          # Flask backend server
├── auth.py                         # Login/register/logout routes
├── models.py                       # Database models (User)
├── train_model.ipynb               # Model training notebook
├── requirements.txt
├── datasets/
│   ├── client_training_dataset.csv     # Labeled dataset used for training
│   └── raw_sensor_new_data.csv         # Unlabeled dataset simulating live sensor input
├── models/                         # Trained model files (generated by the notebook)
│   ├── condition_classifier.pkl
│   ├── rul_regressor.pkl
│   ├── health_regressor.pkl
│   ├── feature_scaler.pkl
│   └── label_encoder.pkl
├── templates/
│   ├── index.html                  # Main dashboard UI
│   ├── login.html
│   └── register.html
└── static/
    ├── css/
    │   └── style.css
    └── js/
        └── main.js
```

---

### How to Run

1. Clone or download the project folder to your local machine.
2. Open a terminal and navigate to the project directory.
3. Install dependencies:

```bash
pip install -r requirements.txt
```

4. (Optional) If the `models/` folder is empty or missing, run all cells in `train_model.ipynb` first to generate the trained model files.
5. Start the Flask server:

```bash
python app.py
```

6. Open your browser and go to `http://127.0.0.1:5000/`. Default admin login: `admin` / `admin123` (change this after first login).

---

### Notes

This project currently uses synthetic (dummy) sensor data for demonstration purposes. Predictions shown on the dashboard (health score, RUL, failure probability) are generated by real trained models, but the input sensor readings are simulated rather than from live industrial hardware.